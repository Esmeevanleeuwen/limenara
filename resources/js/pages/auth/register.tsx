import { Head, Link, useForm } from '@inertiajs/react';
import { AuthLayout, Field, Errors } from '../../components/ui';
export default function Register() {
  const form = useForm({ name: '', email: '', password: '', password_confirmation: '' });
  return <AuthLayout title="Een eigen plek." description="Je hebt geen verwijzing of diagnose nodig om een account te maken."><Head title="Account maken"/><form onSubmit={(e) => { e.preventDefault(); form.post('/register', { onFinish: () => form.reset('password', 'password_confirmation') }); }}><Field label="Naam of schermnaam" name="name" autoComplete="nickname" maxLength={120} value={form.data.name} onChange={(e) => form.setData('name', e.target.value)} required/><Field label="E-mailadres" name="email" type="email" autoComplete="username" value={form.data.email} onChange={(e) => form.setData('email', e.target.value)} required/><Field label="Wachtwoord · minimaal 12 tekens" name="password" type="password" autoComplete="new-password" minLength={12} value={form.data.password} onChange={(e) => form.setData('password', e.target.value)} required/><Field label="Herhaal je wachtwoord" name="password_confirmation" type="password" autoComplete="new-password" value={form.data.password_confirmation} onChange={(e) => form.setData('password_confirmation', e.target.value)} required/><Errors errors={form.errors}/><p className="fineprint">Je bevestigt hierna jouw e-mailadres. Je krijgt een gewoon gebruikersaccount, geen medewerkerrechten. Lees <Link href="/privacy">wat deze testversie opslaat</Link>.</p><button className="button wide" disabled={form.processing}>Account maken →</button></form><p className="small">Al een account? <Link href="/login">Inloggen</Link></p></AuthLayout>;
}
